<Page x:Class="AutoApp.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AutoApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Авторизация">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="70"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0"
                    Width="300"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center">
            <Label Content="Логин:"></Label>
            <TextBox x:Name="LoginTextBox"></TextBox>
            <Label Content="Пароль:"></Label>
            <PasswordBox x:Name="PasswordBox"></PasswordBox>
        </StackPanel>

        <Button x:Name="RegButton" VerticalAlignment="Center"
                HorizontalAlignment="Left" Click="RegButton_Click"
                Content="Регистрация" Grid.Row="1"></Button>
        <Button x:Name="LoginButton" VerticalAlignment="Center"
                HorizontalAlignment="Right" Click="LoginButton_Click"
                Content="Войти" Grid.Row="1"></Button>
    </Grid>
</Page>