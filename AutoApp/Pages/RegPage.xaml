<Page x:Class="AutoApp.Pages.RegPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AutoApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Регистрация">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="70"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0"
                    Width="300"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center">
            <Label Content="ФИО:"></Label>
            <TextBox x:Name="FIOTextBox"></TextBox>
            <Label Content="Номер телефона:"></Label>
            <TextBox x:Name="PhoneNumberBox"></TextBox>
            <Label Content="Адрес эл.почты:"></Label>
            <TextBox x:Name="EmailTextBox"></TextBox>
            <Label Content="Логин:"></Label>
            <TextBox x:Name="LoginTextBox"></TextBox>
            <Label Content="Пароль:"></Label>
            <PasswordBox x:Name="PasswordBox"></PasswordBox>
        </StackPanel>

        <Button x:Name="BackButton" VerticalAlignment="Center"
                HorizontalAlignment="Left" Click="BackButton_Click"
                Content="Назад" Grid.Row="1"></Button>
        <Button x:Name="RegButton" VerticalAlignment="Center"
                HorizontalAlignment="Right" Click="RegButton_Click"
                Content="Зарегистрироваться" Grid.Row="1"></Button>
    </Grid>
</Page>